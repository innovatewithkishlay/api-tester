import React from "react";

const ApiForm: React.FC = () => {
  return (
    <form className="p-6 rounded-lg bg-white shadow-md">
      {/* Section heading for the request builder */}
      <h2 className="text-2xl font-semibold mb-4">API Request Builder</h2>

      {/* Input fields, selects, and buttons will be added here */}
    </form>
  );
};

export default ApiForm;
